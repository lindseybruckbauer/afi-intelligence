# AFI Wiki — Python dependencies
# Install: pip install -r requirements.txt

# Existing (from prototype)
anthropic>=0.25.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# PDF extraction
pymupdf>=1.24.0           # import fitz — text extraction, layout detection

# Vector store (RAG)
chromadb>=0.5.0           # local persistent vector DB
                          # uses sentence-transformers internally for embedding
                          # first run downloads ~90MB model — works offline after that

# MkDocs (if not already installed)
mkdocs>=1.5.0
mkdocs-material>=9.5.0
